---
import { capacidades } from "../config";
---

<section id="capacidades" class="border-t border-ink-100 py-24 sm:py-32">
  <div class="mx-auto max-w-6xl px-6">
    <div class="max-w-2xl" data-reveal>
      <p class="text-sm font-medium uppercase tracking-widest text-brand-500">Cómo cubrimos tu proyecto</p>
      <h2 class="mt-4 text-3xl font-semibold tracking-tight text-balance sm:text-4xl">
        Cuatro disciplinas en cada proyecto, no un solo programador
      </h2>
      <p class="mt-5 text-lg leading-relaxed text-ink-500">
        Un proyecto de IA que solo se mira desde lo técnico termina en una herramienta que nadie usa.
        Por eso entramos siempre con estas cuatro miradas.
      </p>
    </div>

    <ul class="mt-14 grid gap-5 sm:grid-cols-2">
      {
        capacidades.map((item, i) => (
          <li
            data-reveal
            style={`--d: ${i * 90}ms`}
            class="group relative overflow-hidden rounded-2xl border border-ink-100 p-8 transition-all duration-300 hover:-translate-y-1 hover:border-brand-500/30 hover:shadow-xl hover:shadow-ink-900/5"
          >
            {/* Barrido de color al pasar el cursor */}
            <span
              class="absolute inset-0 -z-10 bg-gradient-to-br from-brand-50 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"
              aria-hidden="true"
            />
            <span
              class="text-sm font-medium tabular-nums text-ink-300"
              aria-hidden="true"
            >
              {String(i + 1).padStart(2, "0")}
            </span>
            <h3 class="mt-3 text-xl font-semibold tracking-tight">{item.titulo}</h3>
            <p class="mt-3 leading-relaxed text-ink-500">{item.texto}</p>
          </li>
        ))
      }
    </ul>
  </div>
</section>
